[package]
name = "kobo-policy"
version.workspace = true
edition.workspace = true
publish = false
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Capability, task, storage, and credential policy for Kobo applications"

[dependencies]
fs4.workspace = true
kobo-abi = { version = "0.5.0", path = "../kobo-abi" }
kobo-dict = { version = "0.5.0", path = "../kobo-dict" }
kobo-json = { version = "0.5.0", path = "../kobo-json" }
kobo-net = { version = "0.5.0", path = "../kobo-net" }
kobo-protocol = { version = "0.5.0", path = "../kobo-protocol" }

[lints]
workspace = true
